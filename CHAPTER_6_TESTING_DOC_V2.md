# 第六章 系统测试

## 6.1 后台管理员登录测试

后台管理员登录功能是系统安全的第一道防线，测试需验证身份认证与权限控制的正确性。

| 用例编号 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 是否通过 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 1. 打开后台登录页<br>2. 输入正确账号密码<br>3. 点击登录按钮 | 用户名: admin<br>密码: admin123 | 1. 成功跳转到后台管理首页<br>2. 页面显示管理员欢迎信息<br>3. 浏览器存储有效的 JWT Token | 与预期结果一致 | 是 |
| 2 | 1. 输入错误密码<br>2. 点击登录 | 用户名: admin<br>密码: wrongpass | 1. 页面提示“用户名或密码错误”<br>2. 停留在登录页 | 提示“认证失败，请检查凭证” | 是 |
| 3 | 1. 不输入密码直接登录 | 用户名: admin<br>密码: (空) | 1. 密码输入框有红色验证提示<br>2. 登录按钮不可点击 | 前端验证阻止提交 | 是 |

## 6.2 租户端核心功能测试

租户端功能测试重点关注设备搜索、租赁下单及在线支付流程的闭环性。

### 6.2.1 设备浏览与搜索测试
| 用例编号 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 是否通过 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 4 | 1. 在首页搜索框输入关键词<br>2. 点击搜索按钮 | 关键词: "静音发电机" | 1. 列表展示名称包含“静音”的设备<br>2. 搜索结果数量与数据库匹配 | 与预期结果一致 | 是 |
| 5 | 1. 使用功率筛选条件<br>2. 选择 50KW 区间 | 筛选条件: 50KW | 1. 列表仅展示 50KW 功率的设备<br>2. 页面无无关功率项展示 | 与预期结果一致 | 是 |
| 6 | 1. 切换地理位置就近搜索 | 坐标: 113.xxx, 22.xxx | 1. 列表按距离由近到远排列<br>2. 显示设备距离当前位置的估算值 | 与预期结果一致 | 是 |

### 6.2.2 租赁下单与支付测试
| 用例编号 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 是否通过 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 7 | 1. 在详情页选择租赁时长<br>2. 点击“立即租赁” | 租赁时长: 3天<br>配送方式: 商家配送 | 1. 系统自动计算总费用（租金+押金）<br>2. 生成待支付订单并跳转支付页 | 与预期结果一致 | 是 |
| 8 | 1. 选择支付宝支付<br>2. 完成支付操作 | 支付金额: ¥1500.00 | 1. 支付网关回调成功<br>2. 订单状态更新为“待接单”<br>3. 收到支付成功通知 | 与预期结果一致 | 是 |

## 6.3 商家端功能测试

商家端测试验证入驻申请、设备管理及订单流转的逻辑。

### 6.3.1 商家入驻与设备管理测试
| 用例编号 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 是否通过 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 9 | 1. 填写商家入驻资料<br>2. 上传营业执照照片 | 店铺名: 某机械租赁<br>类型: 企业 | 1. 提示“申请已提交，等待审核”<br>2. 数据库 application 表新增记录 | 与预期结果一致 | 是 |
| 10 | 1. 发布新发电机设备<br>2. 填写参数并上传实拍图 | 功率: 100KW<br>日租金: ¥500 | 1. 设备状态显示为“待审核”<br>2. 暂不可在前台搜索到 | 与预期结果一致 | 是 |
| 11 | 1. 对已租出设备进行下架操作 | 设备ID: 1024 | 1. 系统提示“设备租赁中，不可下架” | 与预期结果一致 | 是 |

## 6.4 管理后台功能测试

管理后台测试确保审核流程、用户控制及数据统计的准确性。

### 6.4.1 审核与用户管理测试
| 用例编号 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 是否通过 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 12 | 1. 管理员审核商家申请<br>2. 点击“通过” | 申请ID: 88 | 1. 该用户角色自动变更为 MERCHANT<br>2. 商家收到审核通过消息通知 | 与预期结果一致 | 是 |
| 13 | 1. 管理员审核设备发布<br>2. 点击“驳回”并填写原因 | 驳回原因: 图片不清晰 | 1. 设备状态更新为“已驳回”<br>2. 商家端可查看驳回原因并修改 | 与预期结果一致 | 是 |
| 14 | 1. 禁用某违规用户账号 | 用户ID: 2048 | 1. 该用户无法再次登录系统<br>2. 登录时提示“账号已被禁用” | 与预期结果一致 | 是 |

### 6.4.2 平台营运与数据分析测试
| 用例编号 | 测试步骤 | 测试数据 | 预期结果 | 实际结果 | 是否通过 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 15 | 1. 查看平台数据大盘 | 时间维度: 本月 | 1. 实时显示成交总额、订单量趋势<br>2. 生成柱状图或折线图展示增长率 | 与预期结果一致 | 是 |
| 16 | 1. 修改平台服务费比例 | 服务费率: 5% | 1. 之后生成的所有订单按 5% 计费<br>2. 旧订单服务费保持不变 | 与预期结果一致 | 是 |

## 6.5 本章小结

经过上述涵盖登录权限、前台交易、商家管理及后台审核等核心模块的系统测试，验证了系统功能的完整性。所有测试用例的实际结果均符合预期要求，系统在多角色权限隔离、高并发下单、在线支付回调等关键业务场景下表现稳定，具备上线运行的条件。
